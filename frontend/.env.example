# In local dev, leave this unset — Vite proxies /api to http://127.0.0.1:8000 (see vite.config.ts).
# In production (Vercel), set this to your deployed backend's URL, e.g.:
# VITE_API_BASE_URL=https://internsync-api.onrender.com/api
